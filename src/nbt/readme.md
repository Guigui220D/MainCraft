# NBT module

## Intro

This modules handles reading NBT encoded data.

Because a client only needs decoding NBT, this module does not support encoding.

## TODO

- [] comptime defined fast readers for usual templates that happen in packets